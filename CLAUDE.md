# CLAUDE.md — bimanual-robot 팀 저장소 규칙

양팔 로봇 팀 프로젝트의 공용 저장소다. 전역 `~/.claude/CLAUDE.md`를 상속하고, 아래는 이 저장소에 참여하는 모든 작업자에게 적용되는 프로젝트 규칙이다.

## 세션 시작 시
1. [PROGRESS.md](PROGRESS.md) 최상단 엔트리를 읽어 직전 상황을 이어받는다.
2. [README.md](README.md)의 현재 상태 체크박스로 남은 일을 확인한다.
3. 세션 종료 시 PROGRESS.md 최상단에 로그를 append한다.

## 이 저장소의 경계
- **팀 공용 단일 원본**이다. 설계·코드·실험 규격·회의 결정·검증 증거를 팀원이 함께 관리한다.
- 개인 실험은 Issue와 작업 브랜치에서 진행하고, 재현 절차와 로컬 검증 결과를 Pull Request로 공유한 뒤 `main`에 반영한다. 팀원 승인은 필수가 아니며 작업자가 검증 후 직접 병합할 수 있다.
- 개인 메모·자격증명·팀과 무관한 학습 기록은 커밋하지 않는다.
- 팀원 이름·개인 정보는 회의록 원본에 있는 범위(호칭)를 넘겨 적지 않는다.
- 부트캠프 개인 학습 노트는 여기가 아니라 `~/physical-ai-lab/learning/notes/`에 쓴다. 이 저장소는 팀 프로젝트 실행 기록만 담는다.

## 데이터 규칙
- **코드·문서·스키마만 커밋한다.** 데이터셋·모델 가중치·로그는 커밋하지 않는다.
- 텔레옵 에피소드(`*.parquet`, `*.mp4`, LeRobot 데이터셋 디렉터리), 모델(`*.pt *.pth *.safetensors *.onnx`), rosbag(`*.bag *.mcap`)은 `.gitignore` 대상이다.
- 데이터 실물은 **HuggingFace Hub(private)** 에 두고, 어느 repo에 무엇이 있는지는 [data/README.md](data/README.md)의 인덱스로만 추적한다.
- 데이터를 만들 때는 셋을 항상 붙인다: ①수집 조건 메타데이터 ②에피소드 포함/제외 기준과 근거 ③dataset card. 규격은 `data/schema/`에 있다.
- 커밋 전 `git status`로 대용량이 섞이지 않았는지 확인한다.

## 실기체 안전 규칙
- **팔을 움직이는 모든 동작(토크 ON·이동·캘리브레이션)은 사용자 확인을 받고 실행한다.** 읽기 진단만 예외다.
- 이동 실패(스톨)를 감지하면 로그만 남기지 말고 **반드시 정지 명령을 보낸다.** 스톨된 서보는 계속 밀면 탄다.
- 캘리브레이션 파일은 덮어쓰기 전에 타임스탬프 백업을 남긴다.
- 기하(장착 위치·오프셋)는 자작 프로브가 아니라 **줄자와 실물 대조로 확정한다.** 프로브가 실물과 다르면 프로브를 의심한다.
- 성공/실패 판정은 로그 문자열이 아니라 **실좌표·실물 관측**으로 센다.

## 실패 원인 귀속
실패했을 때 위에서부터 배제한다: L1 하드웨어·연결 → L2 좌표·표현 → L3 로직·파라미터 → L4 데이터·분포 → L5 측정.
- 같은 조건에서 반복되는 실패는 L3 로직·파라미터를, 조건에 따라 성공률이 갈리는 실패는
  L4 데이터·분포를 먼저 살필 **판별 단서**다. 하드웨어·연결처럼 항상 실패하는 반례가
  있으므로 이 패턴만으로 원인을 확정하지 않는다.
- 실패 1건으로 고치지 않는다. L4 의심 시 같은 조건 3회 + 다른 조건 3회를 먼저 돌린다.
- 성공 수치는 조건(범위·격자·반복 횟수)과 같은 줄에 적는다.
- `failure_code`에는 관측된 증상만 적고 확인된 원인과 혼용하지 않는다.
- 실패 에피소드에는 `failure_record_id`를 붙이고, 가설·기각 근거·확정 원인·조치·동일 조건
  재시험은 [data/failures/README.md](data/failures/README.md)의 별도 실패 레코드로 남긴다.
- Nav2·TF·캘리브레이션·기구·서보 문제가 배제되기 전에는 정책 데이터 부족으로 단정하거나
  실패 원본을 BC 학습 데이터에 넣지 않는다.

## 커밋 컨벤션
- Conventional Commits + **한글 설명**: `<type>(scope): 한글 설명`
- scope 예: `docs`, `data`, `ik`, `depth`, `il`(이미테이션 러닝), `teleop`, `research`
- 예: `docs(meeting): 킥오프 회의 결정사항 정리`, `feat(ik): 양팔 좌우 IK 솔버 분리`
- **스테이징은 파일 단위로.** `git add .` · `-A` · 디렉터리 통째 금지.
