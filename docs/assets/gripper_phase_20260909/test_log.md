# 실물 테스트 로그 — 2026-09-09 그리퍼 서보 ID 6

터미널 출력을 그대로 옮긴 것이다. 해석은 [사고 분석 문서](../../20260909_그리퍼_서보_Phase반전_사고분석.md)에 있다.
보드: 팔로워 `5B3D046653` (12 V). 서보: STS3215, ID 6, ggao50 랙-피니언 그리퍼.

## 이탈 사고 4건 (발산 감지 도입 전)

| # | 직전 상태 | 명령 | 결과 |
|---|---|---|---|
| 1 | 열림 732 (오프셋 −1745, 한계 763~3600) | `goto --goal 3000 --max-delta 2400 --speed 200` | 열림 쪽으로 달아나 레일 이탈 |
| 2 | 열림 687 (재조립 후) | `goto --goal 950 --max-delta 400 --speed 150` | 열림 쪽으로 달아나 레일 이탈 |
| 3 | 2082 (오프셋 408, 한계 800~3540) | `goto --goal 1200 --max-delta 1200 --speed 150` | 1821 에서 멈춤, 열림 쪽 이탈 |
| 4 | 열림 580 (오프셋 1719, 한계 680~3480) | `goto --goal 2900 --max-delta 2400 --speed 150` | 열림 쪽으로 달아나 레일 이탈 |

공통점: **목표값이 위든 아래든, 시작이 어디든, 물리적으로 항상 열림 끝으로 달아났다.**

## 발산 감지 도입 후 (이탈 0건)

`servo_goto.py` 에 "목표에서 시작 오차보다 20 카운트 이상 멀어지면 즉시 구동 차단" 을 넣은 뒤의 기록.

### 5. 영점 2048 · 한계 370~3180 · 보호값 원래(25%/0.5 s) · Phase 12

```
ID 6  현재 327  목표 800  델타 +473 (+41.6도)  한계 370~3180  온도 35C
  발산: 목표에서 멀어짐 (오차 473->498). 모터-엔코더 극성 반대 의심 -> 정지·구동해제. 위치 294
```
→ 위로 보냈는데 아래로 33 이동. 3 mm 에서 정지, 이탈 없음.

### 6. 오프셋 0 · 한계 0~4095 · 보호값 원래

```
python3 servo_goto.py --port $F --goal 1554 --max-delta 400 --speed 100 --diverge-tol 20 --execute
```
→ 2회 모두 열림 방향, 발산 정지 (출력 미보존). 직후 읽기: 위치 1204, 목표 레지스터 1208.

```
python3 servo_goto.py --port $F --goal 904 --max-delta 400 --speed 100 --diverge-tol 20 --execute
```
→ 반대 방향 목표. 역시 열림 방향, 발산 정지 (출력 미보존).

### 7. 보호값 복원 후 (MaxTq 1000 · OvldTq 80 · ProtTime 200 · ProtCur 320) · Phase 12

```
ID 6  현재 1315  목표 1515  델타 +200 (+17.6도)  한계 0~4095  온도 35C
  발산: 목표에서 멀어짐 (오차 200->223). 모터-엔코더 극성 반대 의심 -> 정지·구동해제. 위치 1290
ID 6  현재 1290  목표 1515  델타 +225 (+19.8도)  한계 0~4095  온도 35C
  발산: 목표에서 멀어짐 (오차 225->247). 모터-엔코더 극성 반대 의심 -> 정지·구동해제. 위치 1266
```
→ 보호값은 원인이 아님.

### 8. Phase 12 → 76 (재부팅 없이)

```
ID 6  현재 2301  목표 2501  델타 +200 (+17.6도)  한계 0~4095  온도 36C
  발산: 목표에서 멀어짐 (오차 200->223). 모터-엔코더 극성 반대 의심 -> 정지·구동해제. 위치 2275
ID 6  현재 2275  목표 2501  델타 +226 (+19.9도)  한계 0~4095  온도 35C
  발산: 목표에서 멀어짐 (오차 226->247). 모터-엔코더 극성 반대 의심 -> 정지·구동해제. 위치 2252
```
→ 그대로. Phase 는 재부팅 후 적용되는 레지스터.

### 9. 12 V 재투입 후 (Phase 76 유지 확인)

```
재부팅 후  Phase 76  MaxTq 1000  TqLim 1000  OvldTq 80  ProtTime 200  위치 2250  구동 0  전압 12.3V
```
```
python3 servo_goto.py --port $F --goal 2450 --max-delta 300 --speed 100 --diverge-tol 20 --execute
```
→ **도달.** 직후 읽기: 위치 2445, 목표 레지스터 2445, 부하 0. 죠는 닫히는 방향으로 움직임.

## 정지 상태 두 점 읽기 (방향·행정 확정용, 서보 무동작)

| 시점 | 오프셋 | 완전 열림 | 완전 닫힘 | 행정 |
|---|---|---|---|---|
| 09-07 | −1075 | 732 | 3600 | 2868 |
| 09-09 오프셋 0 | 0 | 2328 | 1112 | (경계 넘음) 2880 |
| 09-09 영점 2048 | 960 | 327 | 3212 | 2885 |

세 번 모두 행정 ≈ 2870~2885 카운트 ≈ 95 mm 로 일치. 기구는 그대로였다는 증거.

## EEPROM 대조 (수정 전, 팔로워 ID 6 만 다른 항목)

| 주소 | 이름 | F1~F5 | **F6** | 리더 L6 |
|---|---|---|---|---|
| 18 | Phase | 12 | **12** | 76 |
| 33 | Mode | 0 | 0 | 0 |
| 16/17 | Max Torque | 1000 | **500** | 500 |
| 36 | Overload Torque | 80 | **25** | 25 |
| 35 | Protection Time | 200 | **50** | 200 |
| 28/29 | Protection Current | 320 | **250** | 250 |

수정 후 전체 덤프: [`eeprom_after_fix.json`](eeprom_after_fix.json).
오프셋·한계 변경 이력: [`servo_backup_log.jsonl`](servo_backup_log.jsonl).
